name: quiet_luxury_calculator_flutter
description: "Calculator 2026 Pro — Premium calculator with persistent history, scientific mode, unit converter, and beautiful UI."
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  path: ^1.9.1
  provider: ^6.1.2
  flutter_animate: ^4.5.2
  intl: ^0.20.2
  sqflite_common_ffi: ^2.4.2
  sqflite_common_ffi_web: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
